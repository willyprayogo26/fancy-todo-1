const todoController = require('./todos')
const userController = require('./users')
const projectController = require('./projects')

module.exports = { todoController, userController, projectController }